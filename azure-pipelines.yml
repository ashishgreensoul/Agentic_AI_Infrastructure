trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

pool:
  name: Agentic-AI-POOL

variables:
  terraformVersion: '1.14.0'
  workingDirectory: 'environments/dev'

stages:

# =========================================================
# STAGE 1 - TERRAFORM CI
# =========================================================

- stage: Terraform_CI
  displayName: 'Terraform CI'

  jobs:

  - job: Validate
    displayName: 'Terraform Validate'

    steps:

    - checkout: self

    # Install Terraform
    - task: TerraformInstaller@1
      displayName: 'Install Terraform'
      inputs:
        terraformVersion: $(terraformVersion)

    # Terraform Format
    - script: |
        cd $(workingDirectory)
        terraform fmt -check -recursive
      displayName: 'Terraform Format Check'

    # Terraform Init
    - script: |
        cd $(workingDirectory)
        terraform init -backend=false
      displayName: 'Terraform Init'

    # Terraform Validate
    - script: |
        cd $(workingDirectory)
        terraform validate
      displayName: 'Terraform Validate'


# =========================================================
# STAGE 2 - SECURITY SCAN
# =========================================================

- stage: Security_Scan
  displayName: 'Terraform Security Scan'

  dependsOn: Terraform_CI
  condition: succeeded()

  jobs:

  - job: Security
    displayName: 'Terraform Security'

    steps:

    - checkout: self


    # =====================================================
    # Install TFLint
    # =====================================================

    - powershell: |
        $ErrorActionPreference = "Stop"

        Write-Host "Downloading latest TFLint release..."

        $release = Invoke-RestMethod `
          -Uri "https://api.github.com/repos/terraform-linters/tflint/releases/latest"

        $asset = $release.assets |
          Where-Object {
            $_.name -eq "tflint_windows_amd64.zip"
          } |
          Select-Object -First 1

        if (-not $asset) {
          throw "TFLint Windows AMD64 package not found"
        }

        $zip = "$(Agent.TempDirectory)\tflint.zip"
        $dir = "$(Agent.TempDirectory)\tflint"

        Write-Host "Downloading: $($asset.browser_download_url)"

        Invoke-WebRequest `
          -Uri $asset.browser_download_url `
          -OutFile $zip

        if (Test-Path $dir) {
          Remove-Item $dir -Recurse -Force
        }

        Expand-Archive `
          -Path $zip `
          -DestinationPath $dir `
          -Force

        Write-Host "Adding TFLint to PATH..."

        Write-Host "##vso[task.prependpath]$dir"

        & "$dir\tflint.exe" --version

      displayName: 'Install TFLint'


    # =====================================================
    # Run TFLint
    # =====================================================

    - powershell: |
        $ErrorActionPreference = "Stop"

        Set-Location "$(workingDirectory)"

        Write-Host "Initializing TFLint..."

        tflint --init

        Write-Host "Running TFLint..."

        tflint

      displayName: 'Run TFLint'


    # =====================================================
    # Install & Run Checkov
    # =====================================================

    - powershell: |
        $ErrorActionPreference = "Stop"

        Write-Host "Installing Checkov..."

        python -m pip install --upgrade pip
        python -m pip install checkov

        New-Item -ItemType Directory -Force -Path "$(Build.ArtifactStagingDirectory)\security" | Out-Null

        Write-Host "Running Checkov..."

        checkov `
          -d "$(workingDirectory)" `
          --framework terraform `
          -o json `
          --output-file-path "$(Build.ArtifactStagingDirectory)\security\checkov.json"

      displayName: 'Run Checkov'
      continueOnError: true

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Checkov Report'
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)\security'
        ArtifactName: 'security-report'
        publishLocation: 'Container'

    - powershell: |
        $ErrorActionPreference = "Stop"

        Write-Host "Installing Checkov..."

        python -m pip install --upgrade pip
        python -m pip install checkov

        New-Item -ItemType Directory -Force -Path "$(Build.ArtifactStagingDirectory)\security" | Out-Null

        Write-Host "Running Checkov..."

        checkov `
          -d "$(workingDirectory)" `
          --framework terraform `
          -o json `
          --output-file-path "$(Build.ArtifactStagingDirectory)\security\checkov.json"

      displayName: 'Run Checkov'

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Checkov Report'
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)\security'
        ArtifactName: 'security-report'
        publishLocation: 'Container'

# ============================================================
# Terraform Plan
# ============================================================

- stage: Terraform_Plan
  displayName: 'Terraform Plan'

  dependsOn:
  - Terraform_CI
  - Security_Scan

  condition: |
    and(
      succeeded(),
      in(
        dependencies.Security_Scan.result,
        'Succeeded',
        'SucceededWithIssues'
      )
    )

  jobs:

  - job: Plan
    displayName: 'Terraform Plan'

    steps:

    - checkout: self

    - task: TerraformInstaller@1
      displayName: 'Install Terraform'
      inputs:
        terraformVersion: $(terraformVersion)

    - powershell: |
        $ErrorActionPreference = "Stop"

        Set-Location "$(workingDirectory)"

        Write-Host "Initializing Terraform..."

        terraform init

      displayName: 'Terraform Init'

    - powershell: |
        $ErrorActionPreference = "Stop"

        Set-Location "$(workingDirectory)"

        New-Item -ItemType Directory -Force `
          -Path "$(Build.ArtifactStagingDirectory)\terraform" | Out-Null

        Write-Host "Creating Terraform Plan..."

        terraform plan `
          -out="$(Build.ArtifactStagingDirectory)\terraform\tfplan"

      displayName: 'Terraform Plan'

    - powershell: |
        $ErrorActionPreference = "Stop"

        Set-Location "$(workingDirectory)"

        terraform show `
          -no-color `
          "$(Build.ArtifactStagingDirectory)\terraform\tfplan" `
          > "$(Build.ArtifactStagingDirectory)\terraform\terraform-plan.txt"

        Write-Host "Terraform plan exported."

      displayName: 'Export Terraform Plan'

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Terraform Plan'
      inputs:
        PathtoPublish: '$(Build.ArtifactStagingDirectory)\terraform'
        ArtifactName: 'terraform-plan'
        publishLocation: 'Container'